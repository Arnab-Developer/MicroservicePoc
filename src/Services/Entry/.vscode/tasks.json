{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/Entry.Api/MicroservicePoc.Service.Entry.Api.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}